

# Switch interieur naar OpenAI Responses API met `input_fidelity: "high"`

## Wat verandert

**Bestand:** `supabase/functions/showroom-photo-studio/index.ts`

### 1. `callOpenAIImageEdit` (regels 267-328) — volledig vervangen

Van: `/v1/images/edits` met FormData binary blob
Naar: `/v1/responses` met JSON body, `input_fidelity: "high"`, `action: "edit"`

```typescript
async function callOpenAIImageEdit(imageBase64: string, prompt: string): Promise<string> {
  // Detect MIME type from magic bytes
  let mimeType = "image/png";
  if (imageBase64.startsWith("UklGR")) mimeType = "image/webp";
  else if (imageBase64.startsWith("/9j/")) mimeType = "image/jpeg";

  console.log(`callOpenAIImageEdit: Responses API, ${mimeType}, base64 length: ${imageBase64.length}`);

  const response = await fetch("https://api.openai.com/v1/responses", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${OPENAI_API_KEY}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      model: "gpt-image-1",
      input: [{
        role: "user",
        content: [
          { type: "input_text", text: prompt },
          { type: "input_image", image_url: `data:${mimeType};base64,${imageBase64}` }
        ]
      }],
      tools: [{
        type: "image_generation",
        input_fidelity: "high",
        action: "edit"
      }]
    })
  });

  if (!response.ok) {
    const errorBody = await response.json().catch(() => ({}));
    const errorCode = errorBody?.error?.code;
    const errorMsg = errorBody?.error?.message ?? "unknown";
    console.error(`OpenAI Responses API error ${response.status}:`, JSON.stringify(errorBody));
    if (errorCode === "moderation_blocked") {
      throw new Error(`MODERATION_BLOCKED: ${errorMsg}`);
    }
    throw new Error(`OpenAI error (${response.status}): ${errorMsg}`);
  }

  const result = await response.json();
  const imageData = result.output
    ?.filter((o: any) => o.type === "image_generation_call")
    ?.map((o: any) => o.result);

  if (!imageData?.length) throw new Error("No image generated by Responses API");

  console.log("OpenAI Responses API edit completed successfully");
  return imageData[0];
}
```

### 2. `buildInteriorPrompt` (regels 334-440) — inkorten naar Manus' bewezen versie

Verwijder de uitgebreide "role play" introductie (regels 337-346). Behoud alle HARDE EISEN exact zoals ze zijn — die zijn bewezen effectief. Verwijder HARDE EIS #4 (camera display) omdat die zelden voorkomt en prompt lengte reduceert.

### 3. Geen andere bestanden wijzigen

Frontend, storage, routing — alles blijft identiek. Alleen de API call methode en prompt veranderen.

### Samenvatting wijzigingen

| Was | Wordt |
|-----|-------|
| `/v1/images/edits` + FormData | `/v1/responses` + JSON |
| `image[]` binary blob | `input_image` met data-URL |
| Geen `input_fidelity` | `input_fidelity: "high"` |
| Geen `action` | `action: "edit"` |
| `result.data[0].b64_json` | `result.output.filter(type=image_generation_call)[0].result` |

### Na deploy: test met WebP foto

Manus bevestigt dat de implementatie identiek is aan zijn werkwijze. De enige onzekerheid is WebP support via de Responses API — als dat een fout geeft, voegen we PNG conversie toe.

